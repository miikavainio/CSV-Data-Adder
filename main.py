import tkinter as tk
from tkinter import ttk, messagebox
import csv
from datetime import datetime

# Creates the window, title and size
app = tk.Tk()
app.title("NHL GameAdr")
app.geometry("400x315")


# Starts the loop
app.mainloop()